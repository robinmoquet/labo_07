$prefix = 'w-'

for $breakpoint in $breakpoints
    +breakpoint($ + $breakpoint)
        for $i in 2..12
            for $y in 1..$i
                $class = '.' + $breakpoint + '\:' + $prefix + $y + '\/' + $i
                {$class}
                    width: (100 / $i) * $y + '%'
